@{
    ViewData["Title"] = "Password Reset Successful";
}

<section class="hero">
    <div class="container">
        <div class="hero-inner">
            <div class="hero-copy">
                <h1 class="hero-title mt-0">@ViewData["Title"]</h1>
                <p class="hero-paragraph">You can now <a asp-controller="Account" asp-action="Login">Login</a> with your new password.</p>

            </div>
        </div>
    </div>
</section>
